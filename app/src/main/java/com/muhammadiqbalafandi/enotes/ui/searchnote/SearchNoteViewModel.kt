package com.muhammadiqbalafandi.enotes.ui.searchnote

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class SearchNoteViewModel(application: Application) : AndroidViewModel(application) {

//    private val noteRepository: NoteRepository

    init {
//        val databaseDao = NoteDatabase.getDatabase(application).noteDao()
//        noteRepository =
//            NoteRepository(databaseDao)
    }


}